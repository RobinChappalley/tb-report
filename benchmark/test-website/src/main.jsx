import React from "react";
import ReactDOM from "react-dom/client";
import CloudinaryPlayground from "../cloudinary.jsx";
import "./style.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CloudinaryPlayground />
  </React.StrictMode>,
);
